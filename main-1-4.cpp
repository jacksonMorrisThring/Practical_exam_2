#include <iostream>
#include <string>
using namespace std;
#include <iostream>

int* sparse_count(int **vals,int num_rows, int num_cols);

int main(){
    int exmpl = 1;
    int array[] = {1,2,3,4,5};
    int length = 5;


    odd_even(array, exmpl, length);

    return 0;
}